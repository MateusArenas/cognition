# Carregado automaticamente pelo Expo quando roda local (expo start — NODE_ENV=development é o
# padrão do CLI nesse caso). Ver docs/02-setup-e-estrutura.md.
EXPO_PUBLIC_API_URL=http://localhost:3333
