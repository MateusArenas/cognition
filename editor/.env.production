# Canal de produção ainda não existe (nem o backend de produção) — de propósito sem
# EXPO_PUBLIC_API_URL aqui. Preencher quando o backend de produção existir; até lá, publicar
# neste ambiente dispara o alerta de configuração ausente em src/api/http.ts (é o esperado).
